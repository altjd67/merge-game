# Repository Guidelines

## 응답 및 문서 스타일

- 결론부터 짧게 작성한다.
- 변수·함수·속성·클래스 이름은 영어로 작성한다. 코드 주석과 화면 문구는 한국어로 작성한다.
- 클래스와 주요 공개 메서드에는 역할을 설명하는 XML `summary` 주석을 작성한다. 입력·반환값과 주의할 상태 전이는 필요한 곳에 명시한다.
- 문서와 커밋 메시지는 한국어로 작성한다.
- 가정이나 범위 변경이 필요하면 구현 전에 명시한다.

## 프로젝트 목표

이 저장소는 포트폴리오용 Unity 머지 보드 MVP다. 상세 범위는 [`MVP_SPEC.md`](MVP_SPEC.md)를 단일 기준으로 삼는다.

- 보드: 7열 × 9행, 총 63칸
- 핵심 루프: 생성 → 드래그 → 합성 → 주문 제출 → 보상
- 저장 대상: 보드, 코인, 주문 완료 상태
- MVP 제외 범위: 서버, 로그인, 결제, 광고, 상점, 튜토리얼, 과도한 아이템·주문 확장

명시적으로 요청되지 않은 기능은 추가하지 않는다.

## 작업 전 확인

1. `MVP_SPEC.md`에서 해당 기능의 완료 조건과 제외 범위를 확인한다.
2. 기존 구현에서 유사한 클래스와 데이터 흐름을 먼저 검색한다.
3. 새 기능은 수정·추가 파일, 책임 분리, 영향 범위, 검증 방법을 짧게 계획한다.
4. 요구사항 해석이 둘 이상이면 임의로 선택하지 말고 사용자에게 확인한다.

## 코드 구조

작은 MVP에 맞춰 아래 책임을 유지한다.

```text
View: 입력 전달과 UI 표시
Controller: 이동, 합성, 주문, 보상 규칙
Model: 보드, 아이템, 주문, 코인 상태
SaveService: 로컬 저장과 복원
```

- View는 게임 규칙이나 저장 로직을 직접 처리하지 않는다.
- Controller는 Unity UI 세부 구현에 의존하지 않는다.
- Model은 Unity `MonoBehaviour` 없이 게임 상태를 표현한다.
- 필요한 최소 단위로만 클래스를 분리하며, 사용처가 하나뿐인 추상화는 만들지 않는다.
- 매직 넘버는 의도를 설명하는 상수 또는 데이터로 관리한다.

## Unity 규칙

- `Assets/` 아래에만 런타임 코드와 에셋을 추가한다.
- 새 Unity 에셋, 스크립트, 프리팹을 추가·이동·삭제할 때는 대응하는 `.meta` 파일을 함께 커밋한다.
- `Library/`, `Logs/`, `Temp/`, `Obj/`, `UserSettings/`는 커밋하지 않는다.
- 외부 패키지는 사용자 승인 없이 추가하지 않는다.
- 코드 변경 후 Unity Editor에서 컴파일 오류가 없는지 확인한다.
- 플레이 가능한 기능은 최소 한 번 직접 실행해 핵심 흐름을 확인한다.

## 저장 규칙

- 상태 변경 직후 저장한다: 생성, 이동/합성, 주문 완료.
- 저장 데이터가 없거나 읽기 실패 시 기본 보드 상태로 안전하게 시작한다.
- 저장 포맷 변경이 필요하면 기존 저장 데이터 처리 방식을 먼저 결정한다.

## Git 규칙

- 의미 있는 변경마다 작은 단위로 커밋한다.
- 커밋 전 `git status`, staged diff, Unity 캐시 포함 여부를 확인한다.
- 커밋 메시지는 이모지와 한국어 제목을 사용한다.

예시:

```text
✨ 7×9 보드 상태 모델 추가
✨ 아이템 드래그와 합성 구현
🐛 잘못된 드롭 시 원위치 복귀 수정
📝 README 실행 방법 추가
```

## 완료 보고

- 수정 파일과 핵심 변경을 요약한다.
- 실행한 검증과 결과를 명시한다.
- 미구현 범위나 후속 작업이 있으면 `MVP_SPEC.md`의 범위와 연결해 짧게 적는다.
