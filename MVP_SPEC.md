# Merge Board MVP 기획서

## Karpathy 자가점검

- 사고: PASS — 포트폴리오에서 확인할 수 있는 합성·주문·저장 루프만 정의한다.
- 단순함: PASS — 아이템 1종 3단계, 주문 3개, 보드 1개로 제한한다.
- 외과적 변경: PASS — 초기 구현에 필요한 기능만 포함하며 상점, 튜토리얼, 서버는 제외한다.
- 목표 주도 실행: PASS — 플레이 가능한 1회 루프와 저장 복원이 완료 조건이다.

## 1. 개요

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | Merge Board MVP |
| 목적 | 머지 게임의 보드 조작, 합성, 주문 완료, 저장·복원 핵심 루프를 검증한다. |
| 플랫폼 | Unity PC 실행 기준. 마우스 입력으로 구현하고 모바일 터치로 확장 가능한 입력 구조를 유지한다. |
| 목표 시간 | 1일 프로토타입 |
| 보드 크기 | 7열 × 9행, 총 63칸 |

## 2. 플레이 목표

플레이어는 생성기에서 아이템을 만들고 같은 단계의 아이템 두 개를 합성한다. 주문에 필요한 최종 아이템을 제출해 코인을 획득한다. 앱을 다시 실행해도 보드와 코인이 유지되어야 한다.

```text
생성기 사용 → 아이템 드래그 → 같은 단계끼리 합성 → 주문 제출 → 코인 획득
```

## 3. 보드 규칙

### 3.1 보드 구성

- 좌측 상단을 `(0, 0)`으로 하는 7열 × 9행 격자다.
- 각 칸은 비어 있거나 아이템 하나를 가진다.
- 첫 실행 시 아이템 4개를 서로 겹치지 않는 칸에 배치한다.
- 아이템을 빈 칸으로 옮길 수 있다.
- 아이템을 다른 아이템이 있는 칸에 놓았을 때, 합성 조건이 맞으면 합성한다.

### 3.2 아이템 단계

| 단계 | ID | 표시 이름 | 생성 가능 여부 |
| --- | --- | --- | --- |
| 1 | `Seed` | 씨앗 | 생성기에서 생성 |
| 2 | `Sprout` | 새싹 | 씨앗 2개 합성 |
| 3 | `Flower` | 꽃 | 새싹 2개 합성 |

### 3.3 합성 규칙

- 같은 단계의 아이템 두 개를 겹치면 한 단계 높은 아이템 하나가 남는다.
- `Flower`는 최종 단계이므로 다른 `Flower`와 합성할 수 없다.
- 서로 다른 단계, 혹은 최종 단계끼리 겹친 드래그는 원래 위치로 되돌린다.
- 합성 성공 시 짧은 확대 효과와 단계 이름 피드백을 표시한다.

## 4. 생성기

- 생성기는 씨앗 하나를 빈 칸에 생성한다.
- 사용 후 2초의 재사용 대기시간이 있다.
- 빈 칸이 없으면 생성하지 않고 `보드에 빈 칸이 없습니다.` 피드백을 표시한다.
- 생성 위치는 빈 칸 중 무작위로 선택한다.

## 5. 주문과 보상

### 5.1 주문 목록

| 주문 ID | 요청 아이템 | 필요 수량 | 보상 코인 |
| --- | --- | --- | --- |
| `Order01` | 새싹 | 1 | 20 |
| `Order02` | 꽃 | 1 | 60 |
| `Order03` | 꽃 | 2 | 150 |

### 5.2 제출 규칙

- 보드에 요구 단계·수량의 아이템이 모이면 해당 주문 슬롯의 `Get` 버튼을 활성화한다.
- `Get` 버튼을 누르면 조건을 다시 검사하고 필요한 보드 아이템을 함께 소비하며 코인을 즉시 지급한다.
- 소비한 아이템의 표시는 보드 위치에서 주문 슬롯으로 날아간다. 클릭 반응과 제출 애니메이션은 Animator를 사용한다.
- 완료된 주문은 `완료` 상태로 표시한다. 주문 교체는 MVP 범위에서 제외한다.
- 수량 부족이나 완료된 주문은 `Get` 버튼을 비활성화하며 상태를 변경하지 않는다. 주문 카드로 드래그하는 제출은 사용하지 않는다.

## 6. UI와 피드백

- 상단: 현재 코인 수
- 중앙: 7×9 보드
- 하단: 생성기 버튼과 재사용 대기시간
- 보드 위 가로 영역: 주문 카드 3개와 각 카드의 `Get` 버튼
- UI 배치는 사용자 제작 `D:\pkmerge-client\Assets\PKMerge\Scenes\Game.unity`를 참고한다. 해당 프로젝트의 코드·구조·기능은 가져오지 않는다.
- 합성 성공: 아이템 확대 및 단계명 표시
- 주문 완료: Animator 기반 아이템 비행, 주문 카드 완료 표시, `+코인` 플로팅 텍스트
- 잘못된 드롭: 원래 위치 복귀와 짧은 흔들림 또는 안내 문구

## 7. 저장·복원

로컬 저장만 구현한다.

- 저장 대상: 각 보드 칸의 아이템 단계, 코인, 주문 완료 상태
- 저장 시점: 아이템 생성, 이동/합성, 주문 완료 직후
- 복원 시점: 게임 시작 시 1회
- 저장 데이터가 없으면 기본 보드와 초기 주문 상태로 시작한다.

## 8. 구현 구조

필요한 책임만 분리한다.

```text
BoardView / OrderView / HUDView
        ↓ 입력·표시
MergeGameController
        ↓ 게임 규칙
BoardModel / OrderModel / SaveData
        ↓ 상태 보관
LocalSaveService
```

- View는 표시와 사용자 입력 전달만 담당한다.
- `MergeGameController`는 이동, 합성, 주문 제출, 보상 지급을 처리한다.
- Model은 보드·주문·코인 상태를 보관한다.
- `LocalSaveService`는 JSON 또는 `PlayerPrefs` 기반 저장과 복원을 담당한다.

## 9. 구현 순서

1. 7×9 보드 표시와 칸 좌표 모델을 만든다.
2. 씨앗·새싹·꽃 아이템 표시와 드래그 이동을 만든다.
3. 같은 단계 2개 합성과 실패 시 원위치 복귀를 만든다.
4. 생성기와 2초 재사용 대기시간을 만든다.
5. 주문 3개, 제출, 코인 보상을 만든다.
6. 합성·주문 완료 피드백을 추가한다.
7. 저장·복원을 구현하고 새 실행에서 검증한다.

## 10. 완료 조건

- 7×9 보드에서 아이템을 드래그해 이동할 수 있다.
- 같은 단계 아이템 2개가 정확히 한 단계 높은 아이템으로 합성된다.
- 생성기 쿨다운, 빈 보드 처리, 잘못된 드롭이 정상 동작한다.
- 요구 수량 충족 시 `Get` 버튼이 활성화되고 클릭 시 Animator 기반 아이템 비행과 함께 주문 3개를 완료하고 코인을 획득할 수 있다.
- 앱 재시작 후 보드, 코인, 주문 상태가 복원된다.
- README에 조작 방법, 구현 범위, 실행 GIF 또는 영상 링크를 추가한다.

## 11. 제외 범위

- 서버 연동, 로그인, 결제, 광고
- 상점, 인벤토리, 튜토리얼, 이벤트
- 아이템 종류 추가, 주문 무한 갱신, 밸런싱
- 고품질 아트·사운드 제작

이 범위는 핵심 플레이 루프를 완성하고 코드 구조를 설명할 수 있도록 의도적으로 제외한다.
