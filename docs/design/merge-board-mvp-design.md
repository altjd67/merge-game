## Karpathy 자가점검

- 사고: PASS — MVP_SPEC.md와 사용자 확정 사항에 따라 Get 제출 및 Animator 피드백의 상태 흐름을 정리했다.
- 단순함: PASS — 보드 하나, 아이템 3단계, 고정 주문 3개와 로컬 저장만 설계한다.
- 외과적 변경: PASS — 기존 Assets가 비어 있음을 확인했으며 구현 파일은 Assets 아래에 한정한다.
- 목표 주도 실행: PASS — 각 완료 조건에 구현 단위와 성공·실패 검증을 연결한다.

## 구현 목표와 제외 범위

기준 문서는 [MVP_SPEC.md](../../MVP_SPEC.md)다. Unity 6000.3.21f1에서 PC 마우스로 생성 → 드래그 이동 → 합성 → 주문 제출 → 보상 루프를 실행하고, 재시작 시 보드·코인·주문 완료 상태를 복원한다.

7열 × 9행 보드, 씨앗·새싹·꽃, 주문 3개, 2초 생성기 대기시간과 기획된 UI 피드백만 구현한다. 서버, 로그인, 결제, 광고, 상점, 인벤토리, 튜토리얼, 이벤트, 추가 아이템, 주문 무한 갱신, 밸런싱, 고품질 아트·사운드는 제외한다. 모바일 실제 대응은 제외하되 입력 좌표를 게임 규칙과 분리한다.

## 보드·아이템·주문·저장 데이터 모델

| 모델 | 데이터와 불변 조건 |
| --- | --- |
| BoardModel | 열 7, 행 9, 길이 63의 칸 배열. 좌측 상단 (0, 0), 인덱스는 행 × 7 + 열. 각 칸은 빈 칸 또는 아이템 하나이며 범위 밖 좌표는 거절한다. |
| 아이템 단계 | 빈 칸 0, Seed 1(씨앗), Sprout 2(새싹), Flower 3(꽃). 별도의 아이템 객체나 에셋 데이터베이스는 만들지 않는다. |
| OrderModel | 고정 ID, 요구 단계, 요구 수량, 보상, 완료 여부. Order01: 새싹 1개/20코인, Order02: 꽃 1개/60코인, Order03: 꽃 2개/150코인. 완료 후 재제출은 거절한다. |
| 게임 상태 | BoardModel, 주문 3개, 0 이상의 코인. 코인 초기값은 0으로 제안한다. 전체 보상 합계는 230이다. |
| SaveData | 포맷 버전 1, 칸별 단계 배열 63개, 코인, 고정 주문 ID별 완료 여부. UI, 드래그 상태, 애니메이션, 생성기 대기시간은 저장하지 않는다. |

첫 실행은 서로 다른 칸에 아이템 4개를 배치한다. 초기 단계·위치는 미지정이므로 첫 행의 앞 4칸에 씨앗을 배치하는 안을 제안한다. 모델은 MonoBehaviour를 상속하지 않는다. 배열 변경은 Controller의 성공한 명령을 통해서만 수행하고 View에는 조회용 상태를 제공한다.

저장은 application persistentDataPath 아래 JSON 파일 하나로 구성한다. 기존 게임 저장 구현은 없다. 누락, JSON 파싱 실패, 미지원 버전, 잘못된 배열 길이·단계·주문 ID·코인 값은 기본 상태로 복원한다. 고정 주문의 완료 여부와 코인 보상 합계도 검증한다. 파일 쓰기는 임시 파일 작성 후 교체하며, 실패 시 기존 파일을 보존하고 저장 실패를 안내한다. 저장 실패로 현재 플레이 상태를 되돌리지 않는다.

## 드래그 이동, 합성, 주문 제출, 저장/복원의 상태 흐름

드래그 시작 시 원본 칸을 기억하고 표시만 포인터를 따라 움직인다. 드롭 전에는 모델을 변경하지 않는다. 드롭 또는 입력 취소 후에는 드래그 상태를 반드시 해제한다.

| 입력 | 판정과 상태 전이 | 표시·저장 |
| --- | --- | --- |
| 빈 칸 드롭 | 유효한 원본 아이템을 목적지로 이동하고 원본을 비운다. | 보드 갱신 후 즉시 저장한다. |
| 같은 단계 드롭 | 서로 다른 칸이고 Seed 또는 Sprout이면 목적지를 다음 단계로 바꾸고 원본을 비운다. | 즉시 저장하고 목적지 확대·단계명을 표시한다. |
| 잘못된 드롭 | 보드 밖, 서로 다른 단계, Flower끼리, 유효하지 않은 원본은 상태를 바꾸지 않는다. 같은 칸 드롭도 상태 변화가 없다. | 원위치 표시를 복구하고 잘못된 드롭에는 안내 문구를 표시한다. 저장하지 않는다. |
| 생성 | 대기시간 종료와 빈 칸을 확인한 뒤 빈 칸 중 무작위 한 곳에 Seed를 배치한다. 성공 시점부터 2초 대기한다. | 즉시 저장하고 대기시간을 표시한다. 꽉 찬 보드는 `보드에 빈 칸이 없습니다.`를 표시하며 상태를 유지한다. |
| 주문 Get 클릭 | 미완료 주문과 보드의 요구 단계·수량을 재검사한다. 성공 시 필요한 아이템 제거, 완료 처리, 보상 지급을 한 번에 수행한다. | 즉시 저장하고 소비한 아이템의 표시를 Animator로 주문 슬롯까지 이동한다. 완료 표시와 +코인 플로팅 텍스트를 보여준다. 부족 수량·중복 제출은 상태 불변이다. |
| 시작 | SaveService가 읽고 검증한 상태 또는 기본 상태를 Controller에 전달한다. 복원은 시작 시 한 번만 수행한다. | 복원이 끝난 상태로 모든 View를 그린다. |

사용자 확정에 따라 세 주문 모두 보드에 요구 수량이 있으면 Get 버튼을 활성화한다. Order03은 꽃 2개 이상일 때 활성화한다. 클릭 시 가장 작은 칸 인덱스부터 필요한 수량만 함께 소비한다. 한 주문 완료 후 다른 주문의 활성화 여부도 즉시 다시 계산한다. 보드 아이템을 주문 카드에 드롭하는 제출과 누적 제출은 사용하지 않는다. 드래그 중에는 Get 클릭을 막아 드래그 표시와 소비 대상이 충돌하지 않게 한다.

클릭 반응, 합성 확대, 제출 비행 및 보상 피드백은 Animator가 재생하는 AnimationClip으로 구동한다. UI Toolkit 표시에는 Animator가 제어하는 MonoBehaviour의 진행도·배율 값을 전달한다. 비행 시작점과 주문 슬롯 목적지는 View에서 계산하며 Controller는 소비한 칸 목록만 반환한다. 애니메이션은 표시 전용이므로 종료 콜백에서 아이템이나 코인을 변경하지 않는다. 재시작 시에는 이미 저장된 완료 상태를 즉시 표시한다.

Controller는 시간 값을 받아 생성 가능 여부를 판정한다. View의 프레임 갱신은 남은 시간을 표시하기만 한다. 최초 실행 및 복원 직후 생성기는 사용 가능한 상태로 시작하는 안을 제안한다. 애니메이션 중에도 판정은 최신 모델에 따르며, 중복 주문 보상이 발생하지 않도록 완료 여부를 먼저 검사한다.

## View / Controller / Model / SaveService 책임 분리

| 계층 | 책임 |
| --- | --- |
| View | 보드·주문·코인·대기시간 표시, 포인터 입력과 대상 좌표 전달, 드래그 표시, 안내 및 애니메이션. 게임 규칙과 파일 저장은 처리하지 않는다. |
| Controller | 이동·합성·생성·제출·보상 판정, 상태 변경, 성공 직후 저장 요청, 표시할 처리 결과 반환. Unity UI 타입에 의존하지 않는다. |
| Model | 칸과 단계, 고정 주문 데이터, 완료 여부, 코인 등 게임 상태와 조회. MonoBehaviour·UI·파일 접근에 의존하지 않는다. |
| SaveService | JSON 직렬화, 파일 읽기·쓰기, 저장 데이터 유효성 검사, 기본 상태 복원 및 실패 결과 전달. UI에 직접 접근하지 않는다. |

진입용 MonoBehaviour가 Controller와 SaveService를 구성하고 View를 연결한다. 이미 제공되는 UI Toolkit을 사용해 외부 패키지 추가 없이 보드와 카드를 구성한다. 포인터 이벤트는 칸 인덱스, Get 클릭은 주문 ID로 변환해 Controller에 전달한다. 한 사용처뿐인 인터페이스나 범용 이벤트 버스는 도입하지 않는다. UI 배치는 참조 Game 씬의 1080×1920 기준 상단 HUD, 보드 위 가로 주문 영역, 중앙·하단 보드 배치를 따른다. 참조 씬의 RectTransform 배치만 확인하며 코드·구조·에셋은 재사용하지 않는다. MVP 밖의 상점·로비 버튼은 추가하지 않는다.

## 추가·수정할 파일 목록과 각 파일의 책임

아래는 계획 파일이며 실제 책임이 겹치면 같은 작업 단위 안에서 최소한으로 조정한다. 모든 신규 Unity 폴더·파일은 대응하는 .meta를 포함한다.

| 파일 | 책임 |
| --- | --- |
| docs/design/merge-board-mvp-design.md | 본 설계와 검증 대응 관계. 최초 커밋에는 이 문서만 포함한다. |
| Assets/MergeBoard/Scripts/Model/BoardModel.cs | 크기·좌표·칸·아이템 단계와 초기 배치. |
| Assets/MergeBoard/Scripts/Model/OrderModel.cs | 고정 주문 정의와 완료 상태. |
| Assets/MergeBoard/Scripts/Model/GameState.cs | 보드·주문·코인의 게임 상태 묶음. |
| Assets/MergeBoard/Scripts/Model/SaveData.cs | 버전을 포함한 직렬화 데이터. |
| Assets/MergeBoard/Scripts/MergeGameController.cs | 모든 게임 명령, 결과, 대기시간, 저장 호출. |
| Assets/MergeBoard/Scripts/LocalSaveService.cs | JSON 파일 저장·검증·복원. |
| Assets/MergeBoard/Scripts/View/BoardView.cs | 63칸 표시, 드래그 표시·입력, 합성 피드백. |
| Assets/MergeBoard/Scripts/View/OrderView.cs | 주문 카드 3개, 제출 입력, 완료 표시. |
| Assets/MergeBoard/Scripts/View/HUDView.cs | 코인·생성기·대기시간·안내와 플로팅 텍스트. |
| Assets/MergeBoard/Scripts/View/AnimatorFeedback.cs | Animator가 제어하는 진행도·배율을 UI 표시로 전달하고 종료 시 표시를 정리한다. |
| Assets/MergeBoard/Animations/ | 클릭·합성·비행·보상 AnimationClip과 AnimatorController. 모두 .meta를 포함한다. |
| Assets/MergeBoard/Scripts/MergeGameBootstrap.cs | 런타임 UI 및 서비스 구성, 초기 복원, 입력 연결. |
| Assets/MergeBoard/Scenes/MergeBoard.unity | 실행 진입 컴포넌트를 가진 MVP 장면. |
| Assets/MergeBoard/Editor/MergeBoardVerification.cs | 외부 테스트 패키지 없이 규칙·저장 실패 경로를 재현하는 Editor 검증 진입점. |
| ProjectSettings/EditorBuildSettings.asset | MVP 장면을 실행 장면 목록에 등록한다. |
| README.md | Unity 버전, 장면 실행법, 조작, 구현 범위, 저장 위치, 실제 실행 GIF 링크. |
| docs/media/merge-board-mvp.gif | 직접 실행한 핵심 루프의 기록. 가짜 결과나 자리표시자 링크로 대체하지 않는다. |

## MVP_SPEC.md 완료 조건과 구현·검증 항목의 대응 관계

| 기획 완료 조건 | 구현 | 검증 |
| --- | --- | --- |
| 7×9 드래그 이동 | BoardModel, BoardView, Controller | 63칸·좌표 경계·초기 4개 배치 확인. Play Mode에서 빈 칸 이동 및 보드 밖 드롭 후 원위치 확인. |
| 동일 단계 두 개 합성 | Controller, BoardView | Seed 두 개 → Sprout 하나, Sprout 두 개 → Flower 하나 확인. 원본 제거·목적지 결과 및 다른 단계·Flower끼리 거절 확인. 확대·단계명 직접 확인. |
| 생성기 쿨다운·빈 보드 처리·잘못된 드롭 | Controller, HUDView | 2초 이전 거절·2초 이후 생성, 빈 보드에서 생성 성공, 63칸 점유 시 안내·상태 불변 확인. 생성 위치가 항상 빈 칸인지 확인. |
| 주문 3개 완료와 코인 | OrderModel, Controller, OrderView | 요구 수량 충족·부족 시 Get 활성화·비활성화, 완료 주문 재클릭 거절. 정확한 소비와 20/60/150 보상, 전체 230코인 확인. 클릭·아이템 비행의 Animator 재생, 완료 표시·플로팅 텍스트 확인. |
| 재시작 복원 | SaveData, LocalSaveService | 생성·이동·합성·주문 완료 각각 저장 후 새 실행에서 63칸·코인·완료 상태 비교. 누락·손상·미지원 버전·잘못된 값 복원, 쓰기 실패 시 기존 파일 보존 확인. 검증용 임시 경로를 사용해 개인 저장을 훼손하지 않는다. |
| README 조작·범위·실행 자료 | README, 실행 GIF | 문서대로 장면을 열어 플레이하고 실제 GIF 파일과 상대 링크가 열리는지 확인. |

C# 변경 후에는 unity-verify의 status → editor refresh --compile → console 절차를 실행한다. 기능 단위마다 mvp-review로 기획 범위, 성공·실패 상태, 책임 분리, 검증 근거를 검토한다. 문제 발견 시 이번 사용자 요청에 따라 수정하고 두 검증을 다시 수행한다. 실행하지 못한 항목은 통과로 기록하지 않는다.

## 구현 순서와 작업 단위별 커밋 계획

| 순서·커밋 메시지 | 작업 범위와 커밋 전 확인 |
| --- | --- |
| 1. 머지 보드 MVP 설계 문서 추가 | 지정 문서 읽기, 기존 구현 검색, 본 문서 작성·범위 검토. 문서만 단독 커밋한다. |
| 2. ✨ 7x9 보드와 아이템 상태 모델 추가 | 보드·단계·초기 상태 및 장면·기본 보드 표시. 63칸/초기 배치 검증, Unity 검증, MVP 리뷰. |
| 3. ✨ 아이템 드래그 이동과 합성 구현 | Controller와 드래그 입력, 이동·합성·거절 처리. 성공·경계·실패 경로 및 직접 드래그 검증. |
| 4. ✨ 생성기와 주문 보상 구현 | 2초 대기, 무작위 생성, 고정 주문·Get 버튼·소비·보상·HUD. 중복 보상과 수량 부족 검증. |
| 5. ✨ 보드 진행 상태 저장과 복원 구현 | SaveData와 SaveService, 모든 성공한 상태 변경 직후 저장 연결, 최초 복원. 실제 새 실행 및 잘못된 저장 검증. |
| 6. ✨ 합성 및 주문 완료 피드백 추가 | Animator 기반 클릭·확대·비행·플로팅 텍스트, 단계명·완료 표시·잘못된 드롭 안내 마무리. 빠른 연속 입력 중 표시와 모델 일치 및 전체 루프 직접 검증. |
| 7. README 실행 방법과 구현 내용 추가 | 실행법·범위·저장 위치와 실제 실행 GIF. 전체 완료 조건 최종 리뷰와 링크 확인. |

각 단위는 필요한 최소 파일만 stage하고 git status, staged diff, diff --check, Unity 캐시 미포함, 에셋과 .meta 짝을 확인한다. 커밋 후 git status가 깨끗한지 확인한다. 미완료 검증은 명시하며 검증 실패를 무시하고 다음 기능을 진행하지 않는다. 설계와 README 커밋 메시지는 사용자가 지정한 이모지 없는 제목을 우선한다.

## 주요 가정과 불확실성

- Order03은 보드에 꽃 2개가 있으면 Get을 활성화하고 클릭 시 함께 소비·비행하는 방식으로 사용자 확정됐다. 동일한 제출 방식을 세 주문에 적용한다.
- 초기 아이템은 첫 행의 씨앗 4개, 코인은 0, 생성기 대기시간은 실행마다 초기화하는 안이다. 기획에 명시되지 않은 초기값임을 구현 전에 알린다.
- UI Toolkit, JSON 파일, 프로그램으로 구성하는 UI는 패키지를 추가하지 않는 구현 선택이다. 실제 Editor에서 한국어 글꼴 표시와 포인터 동작을 확인하며, 필요한 외부 에셋·패키지가 생기면 별도 승인을 받는다.
- 기획의 `빈 보드 처리`는 빈 보드의 생성 성공과 생성기 절의 가득 찬 보드 거절을 모두 검증한다.
- 저장 포맷 버전 1 이전 데이터는 없으며 알 수 없는 버전은 기본 상태로 시작한다. 향후 마이그레이션 기능은 이번 범위에서 제외한다.
- 최초 설계 시 Unity 연결이 없었으나 사용자 요구사항 확정 후 D:/merge-game의 Unity 6000.3.21f1 연결이 ready임을 확인했다. 구현 및 unity-verify를 진행한다.
